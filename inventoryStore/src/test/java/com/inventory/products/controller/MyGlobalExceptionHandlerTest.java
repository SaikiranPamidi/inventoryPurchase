package com.inventory.products.controller;

/*@RunWith(SpringRunner.class)
@ExtendWith(MockitoExtension.class)*/
class MyGlobalExceptionHandlerTest {

	/*
	 * @Mock private StockRepository stockRepo;
	 * 
	 * @InjectMocks private StockService stockService;
	 * 
	 * 
	 * @Test void checkException() throws Exception{
	 * 
	 * Stocks stock=new Stocks();
	 * 
	 * Mockito.when(stockRepo.findById(2)).thenThrow(new
	 * ProductNotFoundException(2)); stockService.updateQuantityStocks(stock);
	 * 
	 * //assertEquals("",resultError); Stocks stock=new Stocks(); stock.setId(2);
	 * Mockito.when(stockRepo.findById(2)).thenThrow(new
	 * ProductNotFoundException(2)); assertEquals("Product not found 2",
	 * stockService.updateQuantityStocks(stock)); }
	 */

}